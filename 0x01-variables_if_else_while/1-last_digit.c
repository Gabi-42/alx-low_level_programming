#include <stdio.h> 
/**  
 * main - Entry point
 * Return : Always 0 Success)
 */
int main(void)
{
int n;
int Q;
char str[] = "Last digit of"
Q = n % 10;
	if (n > 5)
		printf("%s %d is %d and is greater than 5", n, q, char str[]);
	if else (n == 0)
		printf("%s %d is %d and is 0", n, q, char str[]);
	if else (n < 6 && n != 0)
		printf("%s %d is %d and is less than 6 and not 0", n, q, char str[]);
	return (0);
}
