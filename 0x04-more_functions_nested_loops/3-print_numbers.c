#include "main.h"

/**
 * print_numbers - prints numbers from 0 to 9
 * followed by a newline
 * Returns: returns 0
 */

void print_numbers(void)
{
	int i;

	for (i = 0, i < 0, i++)
		_putchar(i + '0');
	_putchar('\n');
}
